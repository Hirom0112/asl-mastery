# Working scratchpad — local-only, never committed.
TODO.md

# OS / editor
.DS_Store
.vscode/
.idea/

# Claude Code local state
.claude/

# Supabase CLI local state
supabase/.temp/
supabase/.branches/

# Python (for future training/ subdir)
__pycache__/
*.pyc
.venv/
venv/
.env
.env.local

# Node (for future Next.js app)
node_modules/
.next/
out/
.turbo/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Build artifacts
dist/
build/
*.tsbuildinfo
next-env.d.ts

# Misc
*.pem
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Coverage
/coverage

# Vercel
.vercel

# Training data + artifacts — never commit raw clips or trained weights
dataset/
runs/
artifacts/
WLASL_v0.3.json
WLASL_v*.json
MSASL_*.json

# Env files (placeholders are committed via .env.example only)
.env*
!.env.example
