# Python deps for the training pipeline.
# Pinned conservatively for reproducibility (per ARCHITECTURE §2.5).

# Phase 3d — public dataset ingestion
# yt-dlp 2026.3.17+ required: YouTube's recent signature-solver challenge
# needs the --remote-components ejs:github flag, which earlier versions
# do not understand. A local JS runtime (deno or bun) must also be
# installed; the ingestion script invokes yt-dlp with deno-compatible
# remote-components.
yt-dlp>=2026.3.17

# Phase 3f — cleaning (ffmpeg normalize → MP4 + manifest)
# MediaPipe removed on 2026-05-20 under ADR 0010 (reversal of ADR 0006).
# The cleaning pipeline outputs per-clip MP4s and the training-time
# dataset loader (see training/classifier/dataset_video.py from T4)
# reads MP4s directly with torchvision.io / decord.
numpy>=1.26,<2.0
opencv-python-headless==4.10.0.84
imageio[ffmpeg]>=2.34

# Phase 4 — training
torch==2.4.1
torchaudio==2.4.1
torchvision==0.19.1
onnx==1.17.0
onnxruntime==1.20.0
wandb==0.18.5

# Reproducibility
tqdm>=4.66
pyyaml>=6.0
