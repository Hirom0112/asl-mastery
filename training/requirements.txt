# Python deps for the training pipeline.
# Pinned conservatively for reproducibility (per ARCHITECTURE §2.5).

# Phase 3d — public dataset ingestion
yt-dlp==2024.9.27

# Phase 3f — cleaning + MediaPipe extraction
mediapipe==0.10.18  # must stay in sync with MEDIAPIPE_VERSION in lib/mediapipe/loader.ts
numpy>=1.26,<2.0
opencv-python-headless==4.10.0.84
imageio[ffmpeg]>=2.34

# Phase 4 — training
torch==2.4.1
torchaudio==2.4.1
torchvision==0.19.1
onnx==1.17.0
onnxruntime==1.20.0
wandb==0.18.5

# Reproducibility
tqdm>=4.66
pyyaml>=6.0
